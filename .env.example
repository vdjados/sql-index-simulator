# Подключение к PostgreSQL (скопируйте в .env и задайте свои значения).
# При ошибке "failed SASL auth" (28P01) проверьте, что DB_PASS совпадает с паролем пользователя в БД.

DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=mydb
